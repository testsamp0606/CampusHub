rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all collections and subcollections
    // for any authenticated user. This is a permissive rule for development.
    match /{path=**} {
      allow read, write: if request.auth != null;
    }
  }
}
